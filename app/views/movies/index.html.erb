<div class="page-header">
  <h1>
    All Movies
  </h1>
</div>
<p><a href="movies/new_form" class="btn btn-primary">Add a new movie</a></p>

<table class="table table-hover">

  <thead>
    <tr>
      <th>Title</th>
      <th>Year</th>
      <th>Duration</th>
      <th>Description</th>
      <th>Image</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>

    <%= @movies.each do |movie| %>

    <tr>
      <td><img src="<%= movie.image_url %>" width ="80"</td>
      <td><%= movie.title %></td>
      <td><%= movie.year %></td>
      <td><%= movie.duration %></td>
      <td><%= movie.description %></td>
      <td>

      </td>
      <td>

      </td>
      <td>

      </td>
    </tr>

    <% end %>

  </tbody>

</table>

<!-- before you end the loop, create buttons for "Show Details, Edit, and Delete" -->
